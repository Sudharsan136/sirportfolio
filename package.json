{
  "name": "sirportfolio",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\" \"node backend/server.js\"",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix admin && npm install --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
